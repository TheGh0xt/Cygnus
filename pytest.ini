[pytest]
testpaths = tests
# `python -m pytest` puts the repo root on sys.path but the bare `pytest`
# console script does not, so tests importing `src.*` passed locally and
# failed under any other invocation (including CI). This makes both work.
pythonpath = .
asyncio_mode = auto
